<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>saintstir | search. discover. venerate.</title>
    <%= csrf_meta_tags %>
    <%= favicon_link_tag %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <!-- Le styles -->
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <!-- Javascripts -->
    <%= javascript_include_tag "application" %>
    <style>
      body {
        padding-top: 80px; /* 60px to make the container go all the way to the bottom of the topbar */
      }
      .black-caret {
        border-top-color: black !important;
        border-bottom-color: black !important;
      }
    </style>

    <%#// Google Analytics usage integration into site %>
    <%= render :partial => 'layouts/ga' if (Rails.env == "production") %>

  </head>

  <body>

    <!--// Top Navbar (do different things depending on user's logged in status and roles) -->
    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
          <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="i-bar"></span>
            <span class="i-bar"></span>
            <span class="i-bar"></span>
          </a>
          <a class="brand" href="/">
            <h2>
              <%= image_tag "logo_white.jpg", {:size => "38x31"} %>
              saintstir
            </h2>
          </a>
          <div>
            <h4>
            <ul class="nav pull-right">
              <li><%= link_to "Explore", saints_path %></li>
              <li class="divider"></li>

              <% if (current_user && current_user.super_user?) %>
                <li class="dropdown" id="menu1">
                  <a class="dropdown-toggle" data-toggle="dropdown" href="#menu1">
                    Admin<b class="caret black-caret"></b>
                  </a>
                  <ul class="dropdown-menu">
                    <li><%= link_to "Saint Admin", admin_saints_path %></li>
                    <li><%= link_to "Postings Admin", admin_postings_path %></li>
                  </ul>
                </li>
                <li class="divider"></li>
              <% end %>

              <% if (current_user) %>
                <li><%= link_to "My Profile", edit_user_registration_path %></li>
                <li><%= link_to "Sign Out", destroy_user_session_path, :method => :delete %></li>
                <li class="divider"></li>
                <li><a href="#"><%=(current_user.username || current_user.email)%></a></li>
              <% else %>
                  <li><%= link_to "Sign In", new_user_session_path %></li>
                  <li><%= link_to "Sign Up", new_user_registration_path %></li>
              <% end %>
            </ul>
            </h4>
          </div>
        </div>
      </div>
    </div>

    <!--
    <div class="container-fluid">
      <div class="content">
        <div class="row">
          <div class="span6">
          </div>
      </div>
      </div>
   </div>
  -->

  <%
      #  has error message
      if (flash[:error])
        %><div class="alert alert-error"><%=flash[:error]%></div><%
      end
      # has notice (status) message
      if (flash[:notice])
        %><div class="alert alert-info"><%=flash[:notice]%></div><%
      end
  %>

  <div class="container-fluid">
  </div>


    <!--// Main page content -->
    <div class="container-fluid">
        <div class="content">
          <div class="row">
            <div class="span12">
            <!--// Yield to page content -->
            <%= yield %>
            </div>
          </div>
        </div>
    </div>

    <br/><br/><br/>
    <!--// Footer -->
    <div class="container-fluid">
      <div class="content">
        <div class="row">
          <div class="span10">
            <%= link_to "About", statics_about_path %> &nbsp;&nbsp;&nbsp;&nbsp;
            <%= link_to "Contact", contact_us_path %> &nbsp;&nbsp;&nbsp;&nbsp;
            <%= link_to "Volunteer", statics_volunteer_path %>   &nbsp;&nbsp;&nbsp;&nbsp;
            <a href="http://twitter.com/saintstir">@Saintstir</a>
          </div>
          <div class="span2">
            &copy; Saintstir <%=Time.now.to_date.strftime("%Y")%>
          </div>
        </div>
      </div>
    </div>

    <!-- Le javascript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

    <script>
      $(document).ready(function() {
        $('.dropdown-toggle').dropdown();
      });
    </script>
  </body>
</html>
